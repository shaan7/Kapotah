#ifndef POINTERS_H
#define POINTERS_H

struct Pointers
{
    class Server *server;
    class FileServer *fserver;
    class PeerManager *manager;
    class FileStatusDialog *fileStatusDialog;
};

#endif // POINTERS_H
